// CustomTextInput.styles.js
import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  input: {
    backgroundColor: '#EDE6F7', // Default background color
    paddingVertical: 10,
    paddingHorizontal: 15,
    fontSize: 14,
    color: '#333',
    borderBottomWidth: 1,
    borderBottomColor: '#6F5E76', // Default bottom border color
    textAlignVertical: 'top', // Keeps text aligned to the top for multiline
  },
});
